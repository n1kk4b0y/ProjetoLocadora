# Copie este arquivo para .env.local e preencha com os dados da sua conexão
# Exemplo local (PostgreSQL instalado na máquina):
DATABASE_URL=postgresql://usuario:senha@localhost:5432/aluguel_carros

# Exemplo usando Neon / Supabase (nuvem):
# DATABASE_URL=postgresql://usuario:senha@host-da-nuvem/aluguel_carros?sslmode=require
